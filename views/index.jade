extends layout

block content
    form(method='POST', action="/users/upload", enctype='multipart/form-data')
        input(type="text", name="user", placeholder="User ID")
        input(type="text" name="sessionToken", placeholder="Session token")
        input(type='file',name="payloads", multiple)
        input(type='submit',value='submit')
